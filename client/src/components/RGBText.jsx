function RGBText({ className, spanText }) {
  // return the `span` tag that can be used
  return <span className={className}>{spanText}</span>;
}

// export as reusable component
export default RGBText;
